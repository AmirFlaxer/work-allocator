# PWA - תוכנית מימוש

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** התקנת האתר כאפליקציה על מסך הבית - manifest + service worker + אייקונים, בדפוס rental_journal, בלי push.

**Architecture:** קבצים סטטיים ב-public (manifest.json, sw.js, אייקונים מרונדרים מ-logo.svg) + רישום SW ב-main.tsx (prod בלבד) + תגיות ב-index.html. אפס תלויות חדשות.

**Tech Stack:** Vite + React (קבצי public מוגשים כמו-שהם). Chrome headless לרינדור האייקונים.

**Spec:** `docs/superpowers/specs/2026-07-19-pwa-design.md`

## Global Constraints

- עברית ב-UI/הערות, מקף רגיל ( - ), בלי חצים.
- **branch:** `feat/pwa` בתיקיית הפרויקט הראשית. בדיקת `git branch --show-current` בפתיחה - עצור אם לא תואם.
- שערים: `npx tsc --noEmit && npm test && npm run lint` (baseline 8 אזהרות, בלי חדשות) `&& npm run build`.
- קריאות Supabase לעולם לא נשמרות ב-cache (ה-fetch handler שומר רק סיומות נכסים סטטיים).
- צבעים: theme_color `#0e6c64` (ה-primary הטורקיז, hsl(175 77% 24%)) · background_color `#fbfbf8` (hsl(48 30% 98%)).

---

### Task 1: כל רכיבי ה-PWA

**Files:**
- Create: `public/manifest.json`, `public/sw.js`, `public/icon-192.png`, `public/icon-512.png`, `public/apple-touch-icon.png`
- Modify: `index.html` (head), `src/main.tsx` (רישום SW)

- [ ] **Step 1: אייקונים מ-logo.svg**

רינדור `public/logo.svg` ל-PNG בגדלים 192/512/180. הדרך הזמינה בסביבה: Chrome headless עם דף HTML זמני בתיקיית ה-scratchpad שמציג את ה-SVG בגודל מדויק ו-`--screenshot`. עבור icon-512 (maskable) - הלוגו בגודל ~80% ממורכז על רקע `#0e6c64` (אזור בטוח); עבור icon-192 ו-apple-touch-icon (180) - הלוגו ממלא את הקנבס על אותו רקע. דף לדוגמה (להתאים מידות פר-קובץ):

```html
<!DOCTYPE html><html><head><style>
  html,body{margin:0;width:512px;height:512px;background:#0e6c64;display:grid;place-items:center}
  img{width:410px;height:410px}
</style></head><body><img src="logo.svg"></body></html>
```

```bash
"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu \
  --window-size=512,512 --screenshot="<scratchpad>/icon-512.png" "<scratchpad>/icon512.html"
```

(להעתיק את logo.svg ל-scratchpad לצד ה-HTML; לוודא שהפלט 512x512 בדיוק ואז להעתיק ל-public.)

- [ ] **Step 2: `public/manifest.json`**

```json
{
  "name": "מחלק עבודה שבועי",
  "short_name": "שיבוץ",
  "description": "מערכת לניהול ושיבוץ עובדים לעמדות עבודה עם אלגוריתם שיבוץ אוטומטי",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait-primary",
  "dir": "rtl",
  "lang": "he",
  "theme_color": "#0e6c64",
  "background_color": "#fbfbf8",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" },
    { "src": "/logo.svg", "sizes": "any", "type": "image/svg+xml" }
  ]
}
```

- [ ] **Step 3: `public/sw.js`** (דפוס rental_journal בלי push)

```js
const CACHE_NAME = "work-allocator-v1";
const STATIC_ASSETS = ["/icon-192.png", "/icon-512.png", "/logo.svg"];

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(STATIC_ASSETS))
  );
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  event.waitUntil(
    caches.keys().then((keys) =>
      Promise.all(
        keys.filter((key) => key !== CACHE_NAME).map((key) => caches.delete(key))
      )
    )
  );
  self.clients.claim();
});

// network-first; שומרים ב-cache רק נכסים סטטיים - קריאות Supabase לעולם לא נשמרות
self.addEventListener("fetch", (event) => {
  if (event.request.method !== "GET") return;

  event.respondWith(
    fetch(event.request)
      .then((response) => {
        if (
          response.ok &&
          event.request.url.match(/\.(js|css|png|svg|woff2?)$/)
        ) {
          const clone = response.clone();
          caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
        }
        return response;
      })
      .catch(() => caches.match(event.request))
  );
});
```

- [ ] **Step 4: `index.html`** - להוסיף ב-head (אחרי שורת ה-favicon):

```html
    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#0e6c64" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
```

- [ ] **Step 5: רישום ב-`src/main.tsx`** - להוסיף בסוף הקובץ:

```ts
// רישום service worker - רק בפרודקשן, כדי לא להפריע ל-HMR בפיתוח
if (import.meta.env.PROD && "serviceWorker" in navigator) {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("/sw.js").catch((err) => {
      console.error("רישום service worker נכשל:", err);
    });
  });
}
```

- [ ] **Step 6: שערים** - `npx tsc --noEmit && npm test && npm run lint && npm run build`; לוודא ש-dist מכיל manifest.json, sw.js והאייקונים.

- [ ] **Step 7: Commit**

```bash
git add public/manifest.json public/sw.js public/icon-192.png public/icon-512.png public/apple-touch-icon.png index.html src/main.tsx
git commit -m "feat: PWA - manifest, service worker ואייקונים להתקנה על מסך הבית"
```

---

### Task 2: מדריך משתמש (md + PDF)

**Files:** Modify `public/user-guide.md`, Regenerate `public/user-guide.pdf`

- [ ] **Step 1:** בסוף פרק "התחברות וחשבון" (אחרי סעיף "מנהל נוסף בארגון") להוסיף:

```markdown
### התקנה על מסך הבית

האתר ניתן להתקנה כאפליקציה: בטלפון, פתחו את האתר בדפדפן ובחרו "הוסף למסך הבית"
(או "התקנה" בתפריט הדפדפן). ייווצר אייקון שנפתח במסך מלא כמו אפליקציה רגילה.
```

- [ ] **Step 2:** יצירת ה-PDF מחדש (התהליך הרגיל - marked + עטיפת RTL + Chrome headless; תבנית מלאה ב-`docs/superpowers/plans/2026-07-18-export-empty-slots-warning.md` Task 2).
- [ ] **Step 3:** אימות ויזואלי (Read ל-PDF).
- [ ] **Step 4: Commit** - `docs: מדריך משתמש - התקנה על מסך הבית (PWA)`

---

### Task 3: אינטגרציה (מתזמר)

- [ ] סקירת-ענף (Sonnet - שינוי סטטי בעיקרו).
- [ ] מיזוג ff ל-main, push, פריסה ירוקה.
- [ ] אימות חי: manifest נטען (fetch 200 + parse), SW נרשם (navigator.serviceWorker.ready ב-Playwright evaluate), האפליקציה עובדת רגיל, האייקונים נגישים.
- [ ] עדכון memory.

## Self-Review (בוצע)

- כיסוי spec מלא (manifest/אייקונים/sw/רישום/index.html - Task 1; מדריך - Task 2; אימות - Task 3).
- אין placeholders; צבעים מפורשים; ה-fetch-handler זהה לתקדים שעובד בפרודקשן של rental_journal.
